function setup(){
    canvas = createCanvas(500, 425);
    canvas.position(600, 150);
    video = createCapture(VIDEO);
    video.size(550, 500);
}
function draw(){
    background("lightBlue");
}
